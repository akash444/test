import java.sql.*;
public class sendToDb{
    
   public void some() throws SQLException{
   
       JdbcConnection js = new JdbcConnection();
       //Connection con2=js.connectJd();
       //Statement stmt=con2.createStatement(); 
       //stmt.executeQuery("insert into student(name,roll) values ('hero',5)");
   
   }
    
}
